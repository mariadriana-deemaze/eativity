{
  "name": "eativity",
  "author": "mariadriana-deemaze",
  "version": "1.0.0",
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "dev:app":"cd app && yarn start",
    "dev:be":"cd backend && yarn start:dev",
    "frontend:lint":"cd app && yarn lint",
    "frontend:format":"cd app && yarn format",
    "backend:e2e": "cd backend && yarn test:e2e"
  }
}
