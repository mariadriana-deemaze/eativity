{
  "name": "eativity",
  "author": "mariadriana-deemaze",
  "version": "1.0.0",
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "scripts": {
    "prepare": "husky install",
    "dev:app":"cd app && yarn start",
    "dev:be":"cd backend && yarn start:dev",
    "fe:lint":"cd app && yarn lint",
    "fe:format":"cd app && yarn format",
    "be:lint":"cd backend && yarn lint",
    "be:format":"cd backend && yarn format",
    "be:e2e": "cd backend && yarn test:e2e"
  }
}
